Component designed to invoke a modflow-2005 docker image.

This component is in draft stage. The inputs of the component have been simplified as a folder, which is assumed to contain all necessary inputs.